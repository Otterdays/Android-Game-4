<!-- PRESERVATION RULE: Never delete or replace content. Append or annotate only. -->
# Game Suggestions — Novel Solutions (v1.2)
> [!NOTE]
> Contributions and restructuring by **Gemini 3 Flash**. Focus: UX > Abstract Elegance.

This document outlines high-impact, technologically feasible game hooks for Godot 4.3 on Android. We prioritize "sensor-native" mechanics and shader-driven visuals to create premium-feeling, "short session" experiences.

---

## 🏗️ Strategic Pillars (Novelty Framework)
Before a mechanic is added, it must pass the **Gemini 3 Flash** "Wow" test:
1. **Sensory Fidelity**: Does it use haptics, gyro, or ambient light in a way that *cannot* be emulated on a PC?
2. **Visual Narrative**: Can a shader tell the story? (e.g., aberration = glitch/health, blur = speed/dizziness).
3. **One-Thumb Mastery**: Infinite depth from a single continuous interaction or discrete taps.

---

## 💎 Tier A — High Hook, Low Lifecycle Cost
| Title | Technical Hook | Novelty Factor | Godot Implementation |
| :--- | :--- | :--- | :--- |
| **Echo Loom** | `Timer` + `Line2D` | Past moves become present obstacles/platforms. | `Curve2D` path storage + periodic instantiation. |
| **Chromatic Drift** | `Post-Process Shader` | Hazards only exist in specific color channels. | Uniform-controlled color splitting; logic tied to `rgb` flags. |
| **Haptic Sculptor** | `Input.vibrate_haptic()` | "Seeing" through touch; find invisible objects. | Proximity-to-target maps to haptic intensity/frequency. |
| **Hold Metropolis** | `Engine.time_scale` | Time = Input. Release to let the world breathe. | `_input` toggles `time_scale` from 0.05 to 1.0. |

---

## 🌌 Tier B — Experimental / Sensor-Heavy
| Title | Technical Hook | Novelty Factor | Godot Implementation |
| :--- | :--- | :--- | :--- |
| **Gyro-Scope** | `Input.get_gyroscope()` | Physical rotation affects gravity *and* resolution. | `ProjectSettings` gravity vector + `Viewport` scale. |
| **Ambient Ghost** | `Light Sensor` (Android) | High light = high difficulty (exposure logic). | Accessing OS-level sensors via Custom Export Template. |
| **Friction Racer** | `InputEventScreenDrag` | Drag length = material friction coefficient. | Calculating vector magnitude over delta-time for impulse. |
| **Symmetry Saboteur** | `ViewportTexture` | Mirrored gameplay; enemies exploit asymmetry. | Two viewports; one inverted with `CanvasLayer` offset. |

---

## 🧪 Phase 3+ Systems (Ambitious)
- **Resonance Miner**: Rhythmic digging. Taps must match the shader's pulse frequency. Uses `AudioServer` analysis in real-time.
- **Shader-Link (Local)**: Screen-space "painting" triggers events on a second device (via Godot-Enet/WebRTC).
- **Bureaucracy Sort**: High-speed pattern recognition with randomized physics-based document "flinging."

---

## 🛠️ Selection Meta-Framework
*Use this rubric to decide what to build first.*

1. **Prototype speed**: Can it be greyboxed in ≤ 4 hours? (If not, simplify).
2. **GIF-ability**: Is the hook obvious without audio or text?
3. **Android-First**: Does it feel "wrong" when played with a mouse?
4. **Shader Synergy**: Does it leverage the current `ChromAb` / `Scanline` direction?

---

## 🚀 Gemini 3 Flash Recommended Shortlist
1. **Haptic Sculptor**: Perfect for Android; minimal assets, maximum "wow" through sensory feedback.
2. **Chromatic Drift**: Leverages your existing shader interest; very high visual polish for low effort.
3. **Gyro-Scope**: Demonstrates technical mastery of the mobile hardware.

---

## 📓 Next Steps
- [ ] Select **one** Tier A idea for the first prototype.
- [ ] Update `ROADMAP_BARE.md` with the choice.
- [ ] Implement the core "Hook" script in `res://scripts/core/`.

---

## 🧠 Gemini 3.1 Pro (High) Expansion: Deep Systems & Advanced Godot 4 
> [!IMPORTANT]
> Generated by **Gemini 3.1 Pro (High)**. This section pushes beyond simple hooks and targets Godot 4's advanced capabilities (Compute Shaders, Advanced 2D Physics, Microphone/OS integration) combined with complex, emergent gameplay loops.

| Title | Technical Hook | Novelty Factor | Godot 4 Implementation |
| :--- | :--- | :--- | :--- |
| **Sonar Weave** | `AudioServer` + `Microphone` | Navigate a pitch-black maze by physically humming/tapping the device mic. Sounds generate bioluminescent visual waves. | `AudioEffectCapture`, mapping audio data to a 2D lighting shader (SDFGI-lite for 2D shadows). |
| **Fluidity Rush** | `Compute Shaders` + `Gyro` | Tilt the phone to guide a highly simulated puddle of 2D liquid. The fluid splits, merges, and reacts to environmental heat. | Utilizing Godot 4's new Compute Shaders for real-time 2D Eulerian fluid dynamics, tied to `get_gyroscope()`. |
| **Axiom Fold** | `Polygon2D` + `Skeleton2D` | Non-Euclidean origami. Use multi-touch to physically "fold" the 2D screen space, aligning esoteric symbols to solve puzzles. | Complex mesh deformation using `Skeleton2D` bones manipulated by `InputEventScreenDrag` (multi-touch). |
| **Chronos Tap** | `Multi-touch` + `Engine.time_scale` | Left thumb holds a collapsing "time-stop" bubble; right thumb plays a high-speed bullet hell. Asymmetric dual-brain engagement. | Separate `CanvasLayer` viewports. The left thumb touch scales Godot's global physics tick, draining a stamina bar. |
| **Geomantic Rift** | `OS Location` (GPS) | A minimalist, walking-based puzzle game. As you physically walk in the real world, the in-game vector grid shifts, aligning ley-lines. | Fetching GPS coordinates via Godot's Android plugins (or mock input), mapping long/lat deltas to a procedural `TileMapLayer`. |

---

## 🔥 Opencode v2-Pro-Free Expansion: Tactile & Procedural Hooks
> [!IMPORTANT]
> Generated by **opencode v2-pro-free**. Focus: procedural generation, touch-native mechanics, and emergent systems using Godot 4's rendering pipeline.

| Title | Technical Hook | Novelty Factor | Godot 4 Implementation |
| :--- | :--- | :--- | :--- |
| **Shatter Garden** | `GPUParticles2D` + `Polygon2D` | Tap to fracture glass-like terrain; shards become collectible seeds that grow procedural flora. Destruction = creation. | Voronoi fracture on `Polygon2D` vertices; shards inherit `RigidBody2D`; growth uses `Curve2D` branching with `Timer` spawning. |
| **Pulse Cartographer** | `AudioStreamMicrophone` + `NavigationRegion2D` | The map doesn't exist until you make sound. Clap, speak, or tap to "sonar-ping" and reveal terrain for a few seconds. | Microphone amplitude triggers visibility on a shader-masked `TileMap`; decay via `lerp` alpha over time. |
| **Ink Tides** | `Line2D` + `ShaderMaterial` | Draw lines that become rivers. Enemies drown in your ink; ink dries and evaporates. Resource management through drawing. | `Line2D` with collision `Polygon2D` generation; shader simulates evaporate via `TIME` uniform; physics queries for drowning. |
| **Fold State** | `SubViewport` + `TextureRect` | Pinch-to-zoom past the surface into a nested reality. Each fold reveals a parallel level with different rules. | Stack of `SubViewports` with transform inheritance; pinch gesture maps to `Camera2D.zoom` with depth-level culling logic. |
| **Gravity Loom** | `Area2D` + `gravity_point_center` | Tap to place gravity wells. Threads of particles weave between wells, forming structures. Destroy the structure to solve the puzzle. | Dynamically spawned `Area2D` with `gravity_point` mode; `GPUParticles2D` field attraction; proximity checks for structure matching. |
| **Void Painter** | `CanvasGroup` + `CompositorEffect` | Everything is black. Paint visibility onto the world with your finger, but the paint bleeds and fades. Exploration as a resource. | `CanvasGroup` masking with a render-texture brush; custom `CompositorEffect` for bleed/smear simulation; `InputEventScreenDrag` sampling. |
| **Echo Slots** | `Timer` + `AnimationPlayer` | Slot-machine combat. Tap to stop reels, but your last 3 taps replay as ghost attacks on the next turn. Memory meets timing. | `AnimationPlayer` driven reel loops; array stores last 3 `Input.is_action_just_pressed` timestamps; ghost entities replay positions. |
| **Magnet Mosh** | `RigidBody2D` + `CollisionShape2D` | Every object has a magnetic polarity. Flip poles with tap; attract/repel chain reactions clear the screen. Physics puzzle. | `RigidBody2D` groups tagged N/S; custom force calculation in `_integrate_forces` using distance vectors; tap toggles group. |
| **Thread Reaper** | `Path2D` + `PathFollow2D` | Enemies follow invisible paths. Cut the path with a swipe to sever their route. Path segments become your defense turrets. | `Path2D` with `curve.split()` on swipe intersection; severed segments instantiate `Area2D` turrets; path preview via `Line2D`. |
| **Breath Bound** | `Microphone` input + `CharacterBody2D` | Blow to jump. Blow harder for higher jumps. Inhale (silence) to descend. Platformer controlled entirely by breathing. | `AudioServer.get_bus_peak_volume()` mapped to `velocity.y`; threshold curves for jump height; inhale state resets via timer. |

---

## 🧬 Opencode Tier B — Procedural & Emergent
| Title | Technical Hook | Novelty Factor | Godot 4 Implementation |
| :--- | :--- | :--- | :--- |
| **Decay Loop** | `ShaderMaterial` + `NoiseTexture` | The level is dissolving in real-time. Terrain erodes via Perlin noise; rebuild with earned fragments. Roguelike urgency. | `FastNoiseLite` in shader drives `discard` on fragment; player deposits rebuild via `ImageTexture` pixel writes. |
| **Signal Chain** | `RayCast2D` + `Line2D` | Drag to connect nodes with laser beams. Beam color determines logic (red=destroy, blue=freeze, green=grow). Circuit puzzle. | `RayCast2D` chain per connection; color uniform drives node behavior via `set_meta` / signal dispatch; `Line2D` visual feedback. |
| **Parallax Thief** | `ParallaxBackground` layers | Swipe to steal depth layers from the background. Shrink the world to reach hidden paths. Spatial puzzle. | `ParallaxLayer` motion scale manipulation; swipe direction shifts `scroll_scale`; collisions re-check on layer changes. |
| **Entropy Arena** | `MultiMeshInstance2D` + `Compute Shader` | 10,000 particle combatants. Tap to drop ordinance. Survivor count drives difficulty scaling. Bullet heaven. | `MultiMeshInstance2D` for rendering; compute shader handles flocking/battle logic; `instance_count` as difficulty lever. |
| **Phase Lock** | `CanvasLayer` + `visibility_layer` bitmask | Two worlds overlap. Toggle which layer you interact with. Objects exist in both but behave differently. | Dual `CanvasLayer` with `visibility_layer` bits; input routing via layer toggle state; cross-layer collision exceptions. |

---

[AMENDED 2026-03-30]: Appended **Gemini 3.1 Pro (High)** expansion pack, introducing deep-system game hooks leveraging advanced Godot 4 features (Compute Shaders, Audio capture, Mesh deformation).
[AMENDED 2026-03-29]: Appended **opencode v2-pro-free** expansion pack — 15 new concepts across tactile/procedural and emergent categories, emphasizing procedural generation, touch-native input, and real-time shader-driven systems.
